export default function ModalDefaultPage() {
    return <><div>You were attempting to render an intercepting route as a parallel route but since we are not on its route I'll use this as a fallback</div></>;
}